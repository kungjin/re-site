<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.yproject.mapper.UserMapper">

  <resultMap id="UserMap" type="com.example.yproject.domain.User">
    <id property="id" column="id"/>
    <result property="email" column="email"/>
    <result property="password" column="password"/>
    <result property="name" column="name"/>
  </resultMap>

  <select id="findAll" resultMap="UserMap">
    SELECT id, email, password, name
    FROM users
    ORDER BY id DESC
  </select>

  <select id="findByEmail" parameterType="string" resultMap="UserMap">
    SELECT id, email, password, name
    FROM users
    WHERE email = #{email}
  </select>

  <insert id="insert" parameterType="com.example.yproject.domain.User"
          useGeneratedKeys="true" keyProperty="id">
    INSERT INTO users (email, password, name)
    VALUES (#{email}, #{password}, #{name})
  </insert>

</mapper>
